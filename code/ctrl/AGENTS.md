# 控制模块仓库技能

在此目录新增拓扑，或修改、重构、审查 cfg/hal/fsm/ctrl 时，先读取仓库技能 [base-ctrl-topology](../../.agents/skills/base-ctrl-topology/SKILL.md)，不要求用户再次显式点名技能。

- 新拓扑按技能的固定结构和函数职责实现；拓扑差异放在采样内容、控制计算、调制回调和必要的生命周期状态中。
- 修改已有模块时，只处理本次范围，不因技能而自动重写其他模块。
- 用户当前明确要求优先。技能不自动授权调整控制参数、修改 `.plecs`、提交或推送。
